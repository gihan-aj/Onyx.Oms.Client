<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Onyx.Oms.Client.Desktop.Features.Orders.List.BulkPrintShippingLabelsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Onyx.Oms.Client.Desktop.Features.Orders.List"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Bulk Print Shipping Labels"
    PrimaryButtonText="Generate Labels"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    Style="{StaticResource DefaultContentDialogStyle}"
    Opened="ContentDialog_Opened">

    <ContentDialog.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:BoolToVisibilityConverter x:Key="InvertedBoolToVisibilityConverter" TrueValue="Collapsed" FalseValue="Visible" />
    </ContentDialog.Resources>

    <Grid Width="500">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Header / Toolbar -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" VerticalAlignment="Center" Spacing="4">
                <TextBlock Style="{StaticResource BodyTextBlockStyle}">
                    <Run Text="Eligible Orders: " Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <Run Text="{x:Bind Orders.Count, Mode=OneWay}" FontWeight="SemiBold"/>
                </TextBlock>
                <TextBlock Text="Fetching up to 200 recent ReadyToPack or Packed orders." 
                           Style="{StaticResource CaptionTextBlockStyle}" 
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>
            <ComboBox Grid.Column="1" 
                      Header="Filter Status"
                      ItemsSource="{x:Bind StatusFilters, Mode=OneWay}" 
                      SelectedItem="{x:Bind SelectedStatusFilter, Mode=TwoWay}"
                      Width="150" Margin="0,0,12,0" VerticalAlignment="Bottom"/>
            <Button Grid.Column="2" 
                    Style="{StaticResource SubtleButtonStyle}" 
                    ToolTipService.ToolTip="Refresh List"
                    Click="RefreshButton_Click"
                    VerticalAlignment="Bottom">
                <FontIcon Glyph="&#xE72C;" FontSize="16"/>
            </Button>
        </Grid>
        <!-- Loading Spinner -->
        <ProgressRing Grid.Row="1" IsActive="{x:Bind IsLoading, Mode=OneWay}" 
                      Width="48" Height="48" VerticalAlignment="Center" HorizontalAlignment="Center" />
        <!-- Order List -->
        <ListView Grid.Row="1" ItemsSource="{x:Bind FilteredOrders, Mode=OneWay}" 
                  Visibility="{x:Bind IsLoading, Mode=OneWay, Converter={StaticResource InvertedBoolToVisibilityConverter}}"
                  SelectionMode="None" 
                  MaxHeight="450" Margin="0,0,4,0"
                  HorizontalAlignment="Stretch"
                  ScrollViewer.VerticalScrollMode="Enabled">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Margin" Value="0,0,0,8" />
                    <Setter Property="Padding" Value="0" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:OrderGridItem">
                    <Grid Background="{ThemeResource LayerFillColorAltBrush}"
                          CornerRadius="8" Padding="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto" MinWidth="40"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,16,0">
                            <TextBlock Text="{x:Bind OrderNumber, Mode=OneWay}" 
                                       Style="{StaticResource BodyStrongTextBlockStyle}" 
                                       TextWrapping="Wrap"/>
                            <TextBlock Text="{x:Bind CustomerName, Mode=OneWay}" 
                                       Style="{StaticResource CaptionTextBlockStyle}" 
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                        <!-- Status Badge -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0,16,0" Spacing="4" Orientation="Horizontal">
                            <Border Style="{StaticResource StatusBadgeStyle}" 
                                    HorizontalAlignment="Center"
                                    Background="{x:Bind Status, Converter={StaticResource OrderStatusBackgroundConverter}}">
                                <TextBlock Text="{x:Bind Status, Mode=OneWay}"
                                           Style="{StaticResource StatusBadgeTextStyle}"
                                           Foreground="{x:Bind Status, Converter={StaticResource OrderStatusForegroundConverter}}" />
                            </Border>
                        </StackPanel>
                        <!-- Remove Button -->
                        <Button Grid.Column="2" 
                                Style="{StaticResource SubtleButtonStyle}" 
                                VerticalAlignment="Center" HorizontalAlignment="Right"
                                ToolTipService.ToolTip="Exclude from printing"
                                Click="RemoveItemButton_Click" Tag="{x:Bind Id}">
                            <FontIcon Glyph="&#xE74D;" FontSize="14" Foreground="{ThemeResource SystemFillColorCriticalBrush}" />
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</ContentDialog>
