<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:sharedconverters="using:Onyx.Oms.Client.Desktop.Shared.Converters">

    <sharedconverters:StockLevelToBackgroundConverter x:Key="StockBackgroundConverter" />
    <sharedconverters:StockLevelToForegroundConverter x:Key="StockForegroundConverter" />
    <sharedconverters:StockLevelToIconConverter x:Key="StockIconConverter" />

    <converters:BoolToObjectConverter x:Key="StatusBackgroundConverter" 
                                      TrueValue="{ThemeResource SystemFillColorSuccessBackgroundBrush}" 
                                      FalseValue="{ThemeResource SystemFillColorCriticalBackgroundBrush}" />

    <converters:BoolToObjectConverter x:Key="StatusForegroundConverter" 
                                      TrueValue="{ThemeResource SystemFillColorSuccessBrush}" 
                                      FalseValue="{ThemeResource SystemFillColorCriticalBrush}" />

    <converters:BoolToObjectConverter x:Key="StatusTextConverter" 
                                      TrueValue="Active" 
                                      FalseValue="Inactive" />

    <Style x:Key="StatusBadgeStyle" TargetType="Border">
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Padding" Value="6,2" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="MinWidth" Value="60" />
    </Style>

    <Style x:Key="StatusBadgeTextStyle" TargetType="TextBlock">
        <Setter Property="Style" Value="{ThemeResource CaptionTextBlockStyle}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="TextAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    
</ResourceDictionary>
