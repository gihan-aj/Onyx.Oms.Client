<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Onyx.Oms.Client.Desktop.Features.Orders.ProcessReturnDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Onyx.Oms.Client.Desktop.Features.Orders"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    Title="Process Return"
    PrimaryButtonText="Process Return"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    Style="{StaticResource DefaultContentDialogStyle}"
    Opened="ContentDialog_Opened">

    <ContentDialog.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:BoolToVisibilityConverter x:Key="InvertedBoolToVisibilityConverter" TrueValue="Collapsed" FalseValue="Visible" />
    </ContentDialog.Resources>

    <Grid Width="500">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Select quantities to add back to inventory:" Margin="0,0,0,16"/>

        <ProgressRing Grid.Row="1" IsActive="{x:Bind IsLoading, Mode=OneWay}" 
                      Width="48" Height="48" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <ListView Grid.Row="1" ItemsSource="{x:Bind ItemsToReturn, Mode=OneWay}" 
                  Visibility="{x:Bind IsLoading, Mode=OneWay, Converter={StaticResource InvertedBoolToVisibilityConverter}}"
                  SelectionMode="None" MaxHeight="300">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Margin" Value="0,0,0,8" />
                    <Setter Property="Padding" Value="0" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:ReturnItemViewModel">
                    <Grid Background="{ThemeResource LayerFillColorAltBrush}" CornerRadius="8" Padding="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind ProductName}" FontWeight="SemiBold"/>
                            <TextBlock Text="{x:Bind Sku}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                        <NumberBox Grid.Column="1" Value="{x:Bind ReturnQuantity, Mode=TwoWay}"
                                   Minimum="0" Maximum="{x:Bind MaxQuantity}" 
                                   SpinButtonPlacementMode="Compact" VerticalAlignment="Center" Width="100"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBox Grid.Row="2" Header="Reason/Notes (Optional)" Text="{x:Bind Reason, Mode=TwoWay}" 
                 Margin="0,16,0,0" AcceptsReturn="True" MinHeight="60"/>
    </Grid>
</ContentDialog>
