<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Onyx.Oms.Client.Desktop.Features.Orders.CustomerOrderHistoryDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Onyx.Oms.Client.Desktop.Features.Orders"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Customer Order History"
    PrimaryButtonText="Close"
    DefaultButton="Primary"
    Style="{StaticResource DefaultContentDialogStyle}">

    <Grid Width="500">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,16" Spacing="4">
            <TextBlock Style="{StaticResource BodyTextBlockStyle}">
                <Run Text="Total Orders: " Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <Run Text="{x:Bind TotalOrdersCount, Mode=OneWay}" FontWeight="SemiBold"/>
            </TextBlock>
            <TextBlock Text="Showing the 20 most recent orders." 
                       Style="{StaticResource CaptionTextBlockStyle}" 
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>
        <ListView Grid.Row="1" ItemsSource="{x:Bind RecentOrders, Mode=OneWay}" 
                  SelectionMode="None" 
                  MaxHeight="450" Margin="0,0,4,0"
                  HorizontalAlignment="Stretch"
                  ScrollViewer.VerticalScrollMode="Enabled"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollMode="Enabled">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Margin" Value="0,0,0,8" />
                    <Setter Property="Padding" Value="0" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:CustomerOrderHistoryItem">
                    <Grid Background="{ThemeResource LayerFillColorAltBrush}"
                          CornerRadius="8" Padding="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto" MinWidth="120"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind OrderNumber, Mode=OneWay}" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                            <TextBlock Text="{x:Bind OrderDateDisplay, Mode=OneWay}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                        <!-- Status Badges -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0,16,0" Spacing="4" Orientation="Horizontal">
                            <Border Style="{StaticResource StatusBadgeStyle}" 
                                    HorizontalAlignment="Center"
                                    Background="{x:Bind Status, Converter={StaticResource OrderStatusBackgroundConverter}}">
                                <TextBlock Text="{x:Bind Status, Mode=OneWay}"
                                           Style="{StaticResource StatusBadgeTextStyle}"
                                           Foreground="{x:Bind Status, Converter={StaticResource OrderStatusForegroundConverter}}" />
                            </Border>
                            <Border Style="{StaticResource StatusBadgeStyle}" 
                                    HorizontalAlignment="Center"
                                    Background="{x:Bind PaymentStatus, Converter={StaticResource PaymentStatusBackgroundConverter}}">
                                <TextBlock Text="{x:Bind PaymentStatus, Mode=OneWay}"
                                           Style="{StaticResource StatusBadgeTextStyle}"
                                           Foreground="{x:Bind PaymentStatus, Converter={StaticResource PaymentStatusForegroundConverter}}" />
                            </Border>
                        </StackPanel>
                        <!-- Financials -->
                        <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                            <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" HorizontalAlignment="Right">
                                <Run Text="{x:Bind GrandTotalCurrency, Mode=OneWay}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <Run Text=" "/>
                                <Run Text="{x:Bind GrandTotalAmount, Mode=OneWay}"/>
                            </TextBlock>
                            <TextBlock Style="{StaticResource CaptionTextBlockStyle}" 
                                       Foreground="{ThemeResource SystemFillColorCriticalBrush}" 
                                       HorizontalAlignment="Right"
                                       Visibility="{x:Bind HasDueBalance, Mode=OneWay}">
                                <Run Text="Due: "/>
                                <Run Text="{x:Bind BalanceAmount, Mode=OneWay}"/>
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</ContentDialog>
