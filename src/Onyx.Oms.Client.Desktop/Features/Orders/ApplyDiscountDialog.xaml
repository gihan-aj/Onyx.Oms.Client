<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Onyx.Oms.Client.Desktop.Features.Orders.ApplyDiscountDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Onyx.Oms.Client.Desktop.Features.Orders"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Apply Discount"
    PrimaryButtonText="Apply"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    PrimaryButtonClick="OnPrimaryButtonClick"
    Style="{StaticResource DefaultContentDialogStyle}">

    <StackPanel Spacing="16" MinWidth="350" Margin="0,12,0,0">
        
        <ComboBox Header="Discount Type" 
                  ItemsSource="{x:Bind DiscountTypes}" 
                  SelectedItem="{x:Bind SelectedType, Mode=TwoWay}"
                  HorizontalAlignment="Stretch" />

        <NumberBox Header="Discount Value" 
                   Value="{x:Bind DiscountValue, Mode=TwoWay}"
                   SpinButtonPlacementMode="Inline"
                   Minimum="0" />

        <TextBox Header="Reason (Optional)" 
                 Text="{x:Bind Reason, Mode=TwoWay}" 
                 PlaceholderText="e.g. Loyalty Customer" />

    </StackPanel>
</ContentDialog>
